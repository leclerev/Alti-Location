{{ form_start(form) }}
    {#{{ form_widget(form) }}#}
    {#<button class="btn">{{ button_label|default('Save') }}</button>#}

    <div class="test">
        {{ form_row(form.type) }}
    </div>

{{ form_end(form) }}
